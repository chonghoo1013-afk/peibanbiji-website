import { defineConfig } from "astro/config";

// Real domain, live on Vercel. Update here if a custom domain is added later.
const SITE_URL = "https://peibanbiji-website.vercel.app";

export default defineConfig({
  site: SITE_URL,
});
